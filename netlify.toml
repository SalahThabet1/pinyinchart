[build]
  command = "CI=false DISABLE_ESLINT_PLUGIN=true react-scripts build"
  publish = "build"

[build.environment]
  NODE_VERSION = "18"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
